# 错误修复记录

日期：2026-09-05

## 验证方式

```bash
npm run build
npm run lint
```

## 已修复的问题

### 1. React Flow 边类型与应用类型不兼容

- **现象**：`npm run build` 在 `Canvas.tsx` 报 TS2769、TS2345 与 TS2322，工作流画布无法构建。
- **原因**：应用自行定义的 `FlowEdge` 将 `sourceHandle` 和 `targetHandle` 设为必填字符串，而 React Flow 的连线允许它们为 `null` 或缺失；`useEdgesState` 返回的 React Flow 边因此无法传入应用状态。
- **修复**：将 `FlowNode` 和 `FlowEdge` 改为 React Flow 的标准 `Node`、`Edge` 类型别名；在创建连线前检查 `source` 和 `target` 是否存在，并保持句柄字段的 React Flow 类型。

### 2. 工具接口实现不完整

- **现象**：`CalculatorTool` 无法注册，构建时报 TS2345 和 TS2420。
- **原因**：`Tool` 接口要求 `parameters`，但 `CalculatorTool` 没有实现该字段。
- **修复**：为计算器工具补充算术表达式的参数 Schema。

### 3. TypeScript 6 不支持构造函数参数属性

- **现象**：构建时报 TS1294。
- **原因**：项目开启 `erasableSyntaxOnly` 后，`ExecutionContext` 中的 `constructor(private readonly ...)` 参数属性不被支持。
- **修复**：将工具注册表声明为普通类字段，并在构造函数中显式赋值。

### 4. 未使用代码导致质量检查失败

- **现象**：构建与 ESLint 分别报 TS6133、TS6196 和未使用变量错误。
- **原因**：节点面板中未使用 `addNode`，`ToolRunner.ts` 仅声明了未使用的类；开始和结束节点也各有未使用的局部变量。
- **修复**：移除无效引用，将 `ToolRunner.ts` 转为空模块，并让开始、结束节点真正使用其计算结果。

### 5. 工作流连线指向不存在的节点时会传入空节点

- **现象**：坏数据或删除节点后的残留连线会使后续执行以 `undefined` 节点继续，错误位置不明确。
- **原因**：引擎查找目标节点后没有验证查询结果，便把可能为空的 ID 传给执行上下文。
- **修复**：在写入目标节点输入前验证目标节点存在；不存在时抛出包含目标节点 ID 的明确错误。

### 6. 开始节点和结束节点没有正确传递工作流数据

- **现象**：开始节点创建的输出未被返回；结束节点无论上游结果是什么都返回固定文本 `你好end`。
- **原因**：两个执行器中存在未使用的临时变量和硬编码返回值。
- **修复**：开始节点返回包含启动信息与变量的输出；结束节点优先返回用户配置的输入，否则返回最近一个上游节点的输出。

### 7. LLM 节点忽略已配置的 Prompt

- **现象**：在编辑器填写 Prompt 后，模型调用只使用上游节点数据。
- **原因**：`LLMExecutor` 只拼接执行上下文中的输入，从未读取 `node.data.inputs.prompt`。
- **修复**：合并节点配置 Prompt 与上游输入；两者均为空时给出明确错误。非字符串上游输出会序列化为 JSON，避免出现 `[object Object]`。

### 8. Mock 模型在空消息历史下会异常访问

- **现象**：空数组调用 `MockModel.invoke` 时读取 `lastMessage.role` 会导致运行时错误。
- **原因**：未验证最后一条消息是否存在，并假定 `content` 始终为字符串。
- **修复**：增加空历史保护；对可选 `content` 使用空字符串回退。

### 9. 条件分支节点没有执行器

- **现象**：画布可以创建“条件分支”节点，但工作流运行到该节点时会报找不到执行器。
- **原因**：节点定义使用了拼写错误的 `coditionNode`，且执行引擎的执行器映射中没有该节点类型。
- **修复**：新增 `ConditionExecutor` 并注册到引擎；节点类型改为 `conditionNode`，同时保留对旧拼写的兼容。它支持 `true`、`false`、`$input`、`$input === <值>` 与 `$input !== <值>`，分别路由至 `true` 或 `false` 句柄；不支持的表达式会给出明确的错误，而不会执行任意代码。

### 10. 服务端模块规范冲突，工作流路由无法匹配

- **现象**：`server` 执行类型检查时，所有 `import` / `export` 报 TS1295；前端请求 `/api/workflow/run` 时，路由前缀也无法正确匹配。
- **原因**：`server/package.json` 声明为 CommonJS，但服务端源码采用 ESM 导入导出，同时 `tsconfig.json` 开启了 NodeNext 与 `verbatimModuleSyntax`；此外，`app.use` 的路由路径遗漏了开头的 `/`。
- **修复**：服务端统一为 ESM（`type: module`），NodeNext 模式下本地导入使用 `.js` 扩展名，并将路由挂载路径改为 `/api/workflow`；同时显式加载 Node 类型定义。

### 11. 工具参数校验会错误放行空的必填字段

- **现象**：工具 Schema 声明某字段为必填时，传入 `{ city: undefined }` 或 `{ city: null }` 仍可能通过校验；数组也会被当作对象参数接收。
- **原因**：原逻辑只检查键是否存在，未检查必填字段的值是否为 `undefined`；类型检查使用宽松比较 `!=`，会同时把 `undefined` 和 `null` 当作“未提供”而跳过校验；JavaScript 中数组的 `typeof` 也是 `object`。
- **修复**：必填字段同时验证键存在且值不是 `undefined`；类型检查改为严格的 `!== undefined`，使 `null` 按实际类型被拒绝；参数入口拒绝数组。额外字段仍保持允许，以兼容当前工具调用设计。

### 12. 服务端 SchemaValidator 与 ToolRunner 无法编译

- **现象**：服务端类型检查出现 TS1005、TS1180、TS1068 等语法错误；项目 ESLint 同时报告 ToolRunner 结果未使用及两个模拟工具参数未使用。
- **原因**：Schema 类型导入遗漏逗号，`validateValue` 调用误写成对象语法；ToolRunner 文件名多了一个点，执行工具后既未校验参数也未返回结果；模拟工具保留接口参数但未读取。
- **修复**：修正 Schema 导入和函数调用语法；将 ToolRunner 恢复为正常文件名，在执行前调用 SchemaValidator，校验失败返回 `INVALID_AGRUMENT`，成功时返回工具执行结果；模拟工具用 `void args` 明确标记参数暂未使用。服务端必填字段校验也同步拒绝 `undefined`。

### 13. 服务端运行时缺少 ExecutionContext 类型

- **现象**：服务端类型检查报 `Cannot find name 'ExecutionContext'`，跨目录导入工作流引擎时还提示 NodeNext 下缺少明确的文件扩展名。
- **原因**：`NodeExecutor` 使用了尚未定义或导入的运行上下文；服务端已经采用 ESM + NodeNext，但工作流引擎导入仍使用无扩展名路径。
- **修复**：新增服务端独立的 `ExecutionContext`，集中保存节点输入输出、变量、消息历史和工具注册表；`NodeExecutor` 显式导入该类型；跨目录 ESM 导入补充 `.js` 扩展名。

### 14. 服务端检查前端共享引擎时产生批量扩展名错误

- **现象**：服务端修复自身语法后，类型检查又在前端 `workflowEngine` 及其所有无扩展名导入处批量报 TS2834、TS2835。
- **原因**：当前服务端路由暂时直接复用 Vite 前端目录中的工作流引擎；前端源码按 Bundler 解析规则组织，而服务端的 NodeNext 规则要求整条导入链都使用 Node ESM 文件扩展名，两套规则发生冲突。
- **修复**：在服务端仍通过 `tsx` 运行、运行引擎尚未完全迁入 `server` 的阶段，将 TypeScript 配置调整为 `ESNext + Bundler`，与共享源码的解析方式保持一致。后续服务端拥有独立工作流引擎后，可再恢复纯 NodeNext 构建。

### 15. 严格类型检查发现条件输入和可选错误码边界问题

- **现象**：服务端检查共享工作流引擎时，条件节点首个输入可能为 `undefined`；工具失败消息在 `exactOptionalPropertyTypes` 下不能把 `undefined` 显式赋给可选 `errorCode`。
- **原因**：仅检查数组长度不能让启用 `noUncheckedIndexedAccess` 的 TypeScript 收窄 `inputs[0]`；工具结果的 `errorCode` 是可选值，但消息对象始终创建该属性。LLM 文本消息还误把最终内容写入 `args`。
- **修复**：先取得并验证 `firstInput` 后再读取输出；仅在错误码存在时才写入消息；LLM 最终文本写入 `assistant.content`。同时修正服务端错误码 `INVALID_AGRUMENT` 的拼写为 `INVALID_ARGUMENT`。

### 16. 工作流业务错误被服务端错误地返回为 500

- **现象**：空工作流没有开始节点时，引擎抛出错误，但 `/api/workflow/run` 返回 `500 INTERNAL_ERROR`，而不是带明确错误码的 400 响应。
- **原因**：路由判断的是服务端定义的 `WorkflowError`，实际复用的前端工作流引擎却只抛普通 `Error`；即使两边都定义同名类，构造函数不同也无法通过 `instanceof`。
- **修复**：建立共享 `WorkflowError` 与统一错误码，工作流引擎在缺少开始节点、执行器、出边、目标节点或超出步数时抛出该类型；服务端重新导出同一个类，确保路由的 `instanceof` 可以正确识别，并修正原错误码中的拼写问题。
- **补充**：共享错误类使用普通字段并在构造函数内赋值，兼容项目开启的 `erasableSyntaxOnly`。
